Raw and processed data live here locally, but are not committed (`.gitignore`).

Use `notebooks/00_download_toy_dataset.ipynb` to download and unzip the FLAIR toy dataset into `data/raw/`. The follow-up EDA notebook reads from that location and summarizes the class distribution.
