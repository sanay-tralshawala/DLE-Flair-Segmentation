"""Dataset, dataloader, and augmentation helpers."""


def build_dataloaders(data_config: dict):
    """Return train, val, and test dataloaders from the configured split CSVs."""
    raise NotImplementedError
