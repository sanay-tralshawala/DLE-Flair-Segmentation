# configs/convnext_tiny_5ch.yaml

defaults:
  - default.yaml

experiment:
  name: convnext_tiny_5ch_val_IoU

data:
  channels: [1, 2, 3, 4, 5]

model:
  name: convnext_tiny
  encoder: convnext_tiny
  in_channels: 5

wandb:
  enabled: true
  group: flair-toy-final-models
  tags:
    - flair
    - segmentation
    - convnext
    - tiny

